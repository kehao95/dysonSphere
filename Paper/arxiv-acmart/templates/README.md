# Template Overrides

This directory holds Pandoc / LaTeX template overrides for the shared
`arxiv-acmart` scaffold.

Current contents:

- [`acmart.latex`](./acmart.latex) as the base Pandoc LaTeX template used by the
  shared build logic
